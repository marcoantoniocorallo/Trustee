// Type System prevents data leakage
let trust pwd = {
  let secret pass = "abcd" in 

  let fun checkpwd (guess : string) : bool = 
    let _ = print_string (declassify(pass)) in 
    declassify(pass = guess) in

  handle: {checkpwd}
} in
pwd.checkpwd "abcd"