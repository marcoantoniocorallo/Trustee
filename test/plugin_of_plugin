// plugin cannot be passed as parameter
let plugin pwd = {
  let pass = "abcd" in 

  let fun checkpwd (guess : string) : bool = 
    pass = guess in
  checkpwd
} in

let filter = <"filter"> in
filter pwd ["pippo", "abc", "abcd", "pluto", "paperino"]